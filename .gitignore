# build output — regenerate with `python3 build.py`.
# docs/ is NOT ignored: GitHub Pages serves it.
dist/

# three.js sources that vendor/three.global.min.js is derived from.
# Only the generated global build is committed, so the page runs off disk;
# see README for how to regenerate it from npm.
vendor/three.module.js
vendor/three.module.min.js

__pycache__/
*.pyc
